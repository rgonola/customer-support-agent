# ─── LLM Provider ────────────────────────────────────────────────────────────
# Choose one: openai, gemini, claude
LLM_PROVIDER=gemini

# ─── Embedding Provider ───────────────────────────────────────────────────────
# Choose one: openai, gemini
EMBEDDING_PROVIDER=gemini

# ─── API Keys ─────────────────────────────────────────────────────────────────
# Provide only the key(s) for your chosen provider(s)
# OPENAI_API_KEY=sk-your-openai-key-here
GOOGLE_API_KEY=AIzaSyDZN3u9JLIBOjJOyL8INWZ35lB99IN1znk
# ANTHROPIC_API_KEY=your-anthropic-key-here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# ─── LLM Configuration ────────────────────────────────────────────────────────
# Gemini models : gemini-2.0-flash | gemini-1.5-pro | gemini-1.5-flash
# OpenAI models : gpt-4o | gpt-4o-mini | gpt-4-turbo
# Claude models : claude-3-5-sonnet-20241022 | claude-3-haiku-20240307
LLM_MODEL=gemini-2.0-flash
LLM_TEMPERATURE=0.0

# ─── Embedding Configuration ──────────────────────────────────────────────────
# Gemini : models/text-embedding-004
# OpenAI : text-embedding-3-small | text-embedding-3-large
EMBEDDING_MODEL=models/text-embedding-004

# ChromaDB Configuration
CHROMADB_PATH=./knowledge_base
CHROMADB_COLLECTION=knowledge_base

# RAG Configuration
RAG_TOP_K=3
RAG_SCORE_THRESHOLD=0.2

# Application Settings
MAX_QUERY_LENGTH=500
RATE_LIMIT_PER_MINUTE=10
SESSION_TIMEOUT_HOURS=24

# Logging
LOG_LEVEL=INFO
LOG_FILE=agent.log

# Security (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000
